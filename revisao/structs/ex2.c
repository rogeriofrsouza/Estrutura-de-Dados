/* 2) Supondo o par de instruções a seguir, desenhe a variável vDados. */
#include <stdio.h>

struct regDados {
  int codigo;
  float preco;
  long long int id;
  int qtde;
};

int main() 
{
  struct regDados vDados;

  return 0; 
}
